{
  "name": "orin-automations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check lib/compiler.mjs && node --check lib/notes.mjs"
  }
}
